server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/juansoto90/microservicedocker.git
          searchPaths: config-data
